const { DataTypes } = require('sequelize');
const cuid = require('cuid');
const sequelize = require('../../config/sequelize');
const User = require('./user.model');

const Sessions = sequelize.define('userSessions', {

})